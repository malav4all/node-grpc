syntax = "proto3";

package todo;


service TodoService {
    rpc getTodos (Empty) returns (TodoList) {}
    rpc createTodo (Todo) returns (Todo) {}
}


message Empty {}


message TodoList {
  repeated Todo todos = 1;
}


message Todo {
   string id = 1;
   string title = 2;
   bool completed = 3;
}